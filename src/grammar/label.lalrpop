LabelType: LabelType = {
    #[precedence(level="2")] #[assoc(side="left")]
    <l:LabelType> "|" <r:LabelType> => LabelType::or(l, r),
    
    #[precedence(level="1")] #[assoc(side="left")]
    <l:LabelType> "&" <r:LabelType> => LabelType::and(l, r),
    
    #[precedence(level="0")]
    "*" => LabelType::Star,
    <Name> => LabelType::Label(<>),
    "(" <LabelType> ")",
};
